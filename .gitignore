# Node modules (don't commit dependencies)
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: OS-specific files
.DS_Store
Thumbs.db

# Optional: Build output (if you ever add tooling)
dist/
build/

# Optional: Environment files (if you ever add any secrets or env vars)
.env
.env.local
.env.*.local

# GitHub Pages cache
.cache/

# VSCode settings (optional — remove if you want these in repo)
.vscode/
